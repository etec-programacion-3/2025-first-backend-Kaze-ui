# Flask y base de datos
flask==2.3.2
flask-sqlalchemy==3.0.3
flask-migrate==4.0.4
python-dotenv==1.0.0

# Autenticación JWT (Fase 2)
flask-jwt-extended==4.4.4
pyjwt==2.7.0
passlib==1.7.4  # Para hash de contraseñas

# Serialización (Marshmallow)
marshmallow==3.19.0
flask-marshmallow==0.15.0

# Validaciones y seguridad
email-validator==1.3.1  # Validar emails (Fase 2)
flask-cors==3.0.10  # Para CORS (si usas frontend)

# Utilidades
python-dateutil==2.8.2  # Manejo de fechas (Fase 3)
requests==2.31.0  # Para APIs externas (opcional)